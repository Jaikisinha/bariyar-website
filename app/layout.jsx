export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <title>Bariyar — Sustainable Jute Products</title>
      </head>
      <body>{children}</body>
    </html>
  );
}
